apiVersion: v1
kind: Service
metadata:
  name: netflix-clone
spec:
  ports:
  - port: 31005
    nodePort: 31010
    targetPort: 80
    protocol: TCP
  selector:
    app: netflix-clone
  type: NodePort
  
